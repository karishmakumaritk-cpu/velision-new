# Copy this to /workspaces/velision-new/server/.env for local development (DO NOT commit real secrets)

# Local dev MongoDB (example)
MONGO_URI=mongodb://127.0.0.1:27017/velision

# Port for server
PORT=5000

# Optional: when you intentionally want to run without trying to connect to a DB
# SKIP_DB_ON_MISSING=true
