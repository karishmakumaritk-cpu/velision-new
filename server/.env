# Local development MongoDB URI (example uses local Mongo)
MONGO_URI=mongodb://127.0.0.1:27017/velision
PORT=5000

# Optional: when you want the server to run without attempting DB connection
# SKIP_DB_ON_MISSING=true

# Example MongoDB Atlas URI (DO NOT commit credentials; keep this only as a reference)
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/velision?retryWrites=true&w=majority
