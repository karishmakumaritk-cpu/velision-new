{
  "name": "velision-new",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "webpack-cli": "^6.0.1"
  }
}
